// @ts-check
import { defineConfig } from 'astro/config';

// 你的 GitHub Pages 地址。
// 默认假设用户名站点 <name>.github.io（site 填你的域名，base 留空）。
// 如果用项目仓库 <name>.github.io/<repo>，把下面 base 取消注释、改成仓库名。
export default defineConfig({
  site: 'https://zsnmfj-tech.github.io',
  // base: '/your-repo',
});
